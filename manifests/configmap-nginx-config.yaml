apiVersion: v1
data:
  default.conf: "server {\n  listen 80;\n  server_name _;\n  \n  location / {\n  \
    \  root /usr/share/nginx/html;\n    index index.html;\n  }\n}\n"
kind: ConfigMap
metadata:
  labels:
    app: nginx-config
  name: nginx-config
  namespace: agentx-agents
